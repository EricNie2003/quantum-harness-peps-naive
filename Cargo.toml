[package]
name = "nqueens-peps-naive"
version = "0.1.0"
edition = "2024"
description = "Exact naive row-by-row sparse contraction of the N-Queens PEPS"
license = "MIT"

[profile.release]
codegen-units = 1
lto = "thin"

