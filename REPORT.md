# N-Queens PEPS 逐行精确收缩：Rust naive baseline

## 1. 结论

本实现完成了一个不做截断、不使用浮点数、也不使用对称性剪枝的 Rust
逐行稀疏 PEPS 收缩基线。在测试机器上，`release` 版本正确复现了
\(Q(4)\) 到 \(Q(14)\) 的全部已知结果；其中：

- \(Q(8)=92\)，三次中位用时 0.233 ms；
- \(Q(12)=14\,200\)，三次中位用时 105.247 ms；
- \(Q(14)=365\,596\)，三次中位用时 4.769 s；
- \(N=14\) 的峰值边界 support 为 5,479,934 个状态；
- 单独一次 \(N=14\) 运行的峰值工作集采样值约为 985.9 MiB。

结果表明实现适合作为正确性和 profiling 基线，但状态 support 和哈希表内存增长很快，
不适合在未优化的情况下直接推进到正式验收所需的大 \(N\)。

## 2. PEPS 与逐行状态的对应

N-Queens PEPS 的每个格点物理变量表示该格是否放置皇后。行、列和两族对角线约束线
沿 bond-dimension-2 的虚指标传播“尚未出现皇后/已经出现皇后”信息。求和格点物理变量后，
完整网络中每个棋盘配置的权重满足：

- 任一约束冲突时，至少一个局域约束张量给出 0；
- 每行、每列恰好一枚且每条对角线至多一枚时，所有局域权重乘积为 1。

因此，对全部物理变量求和即为 \(Q(N)\)。

在一整行收缩完成后，未来行是否还能合法续接，只取决于开放边界上的三组信息：

\[
(C,D_{\leftarrow},D_{\rightarrow})\mapsto w,
\]

其中 \(C\) 是已占用列，\(D_{\leftarrow}\) 和 \(D_{\rightarrow}\) 是下一行受到两族
对角线攻击的位置，\(w\) 是产生该开放虚边界的精确配置数。这三组二值虚指标分别打包为
一个 `u64` mask，系数使用 `u128`。

对输入边界态逐列尝试当前行的物理变量：

1. 若该列出现在 \(C\cup D_{\leftarrow}\cup D_{\rightarrow}\)，对应局域约束权重为 0；
2. 否则权重为 1，加入该列，并将两组对角攻击分别左移、右移后传给下一行；
3. 具有相同输出虚边界的项在哈希表中精确相加。

每步恰好选择当前行的一枚皇后，因此行边界使用“恰好一个”条件；列冲突被 `C`
禁止，最后一行后所有列均被占据；最终剩余对角虚指标按“零或一个均允许”的边界向量求和。
故最终哈希表全部系数之和严格等于完整 PEPS 收缩值。

这是一种对 PEPS 开放虚指标的稀疏表示，并非另写一个递归 DFS：算法按行生成整个边界张量，
相同边界会合并，且不会沿单条棋盘分支回溯。

## 3. 程序设计框架

```text
Cargo.toml
src/
├── lib.rs          # 边界态、逐行 contraction、逐层指标、已知计数
└── main.rs         # solve / bench 命令行入口
benchmarks/
├── naive_release.csv
└── n14_layers.csv
```

核心数据结构：

```rust
struct BoundaryState {
    columns: u64,
    diagonal_left: u64,
    diagonal_right: u64,
}

HashMap<BoundaryState, u128>
```

`contract_rows` 的外层循环对应从上到下收缩 \(N\) 行。每一层创建下一个稀疏边界，
访问每个非零输入态的全部 \(N\) 个物理位置，并记录：

- 输入和输出 support；
- 候选转移与合法转移数；
- 输出总权重；
- 逐层 wall time。

实现特意保持 naive：

- 使用标准库 `HashMap`；
- 每层重新建立哈希表；
- 每个状态显式扫描全部列；
- 单线程；
- 不预分配容量；
- 不做镜像或 \(D_4\) 对称约化；
- 不做有限域、CRT、sort-reduce 或 prefix slicing。

算术全程为整数。`checked_add` 在系数溢出时直接报错，不会静默回绕。
mask 后端目前限制 \(N\le 63\)；实际可达规模首先受内存限制。

## 4. 正确性验证

内置 A000170 已知计数为 \(Q(0)\) 到 \(Q(16)\)。单元测试自动检查
\(Q(0)\) 到 \(Q(10)\)，release benchmark 又逐项检查 \(Q(4)\) 到 \(Q(14)\)。
每个 benchmark 行只有在计算值与常量完全一致时才标记 `verified=true`，否则命令返回失败。

本次结果：

| N | 计算值 | 已知值 | 验证 |
|---:|---:|---:|:---:|
| 4 | 2 | 2 | 通过 |
| 5 | 10 | 10 | 通过 |
| 6 | 4 | 4 | 通过 |
| 7 | 40 | 40 | 通过 |
| 8 | 92 | 92 | 通过 |
| 9 | 352 | 352 | 通过 |
| 10 | 724 | 724 | 通过 |
| 11 | 2,680 | 2,680 | 通过 |
| 12 | 14,200 | 14,200 | 通过 |
| 13 | 73,712 | 73,712 | 通过 |
| 14 | 365,596 | 365,596 | 通过 |

测试命令：

```powershell
cargo test --release
```

结果为 3 个测试全部通过。

## 5. Benchmark 方法与环境

环境：

- CPU：AMD Ryzen 9 7945HX，16 核 / 32 逻辑处理器；
- 内存：34,024,747,008 bytes（约 31.7 GiB）；
- OS/ABI：Windows，`x86_64-pc-windows-msvc`；
- Rust：`rustc 1.94.0 (4a4ef493e 2026-03-02)`；
- LLVM：21.1.8；
- 构建：`cargo run --release`，单线程；
- 每个 \(N\) 独立收缩 3 次，报告中位数和最小值；
- 计时范围只含 `contract_rows`，不含进程启动、CSV 输出和编译；
- 没有单独 warm-up，故同时保留最小值用于观察短任务计时噪声。

执行命令：

```powershell
cargo run --release -- bench 13 --min 4 --repeats 3 --csv
cargo run --release -- bench 14 --min 14 --repeats 3 --csv
```

## 6. Benchmark 结果

| N | Q(N) | 中位时间 (s) | 最小时间 (s) | 峰值状态 | 候选转移 | 合法转移 |
|---:|---:|---:|---:|---:|---:|---:|
| 4 | 2 | 0.000002 | 0.000002 | 6 | 60 | 16 |
| 5 | 10 | 0.000004 | 0.000004 | 14 | 210 | 51 |
| 6 | 4 | 0.000014 | 0.000011 | 43 | 804 | 148 |
| 7 | 40 | 0.000035 | 0.000033 | 145 | 3,304 | 530 |
| 8 | 92 | 0.000233 | 0.000202 | 538 | 14,192 | 1,980 |
| 9 | 352 | 0.000824 | 0.000756 | 2,153 | 64,962 | 8,008 |
| 10 | 724 | 0.004118 | 0.003858 | 8,838 | 308,110 | 33,764 |
| 11 | 2,680 | 0.018477 | 0.018138 | 39,307 | 1,565,047 | 156,885 |
| 12 | 14,200 | 0.105247 | 0.105076 | 188,100 | 8,461,752 | 789,394 |
| 13 | 73,712 | 0.723560 | 0.719082 | 978,362 | 47,909,758 | 4,201,149 |
| 14 | 365,596 | 4.768862 | 4.710589 | 5,479,934 | 286,010,088 | 23,859,616 |

完整机器可读数据见 `benchmarks/naive_release.csv`。

## 7. 逐层行为与瓶颈

\(N=14\) 的输出 support 在第 10 行达到峰值 5,479,934；计算时间峰值出现在第 11 行，
该层输入正是上一层的峰值 support。最后几行约束变强，support 快速下降，但此前已经支付了
哈希表扩展和内存成本。

代表性逐层数据：

| 收缩后行数 | 输入状态 | 候选转移 | 合法转移 | 输出状态 | 层时间 (s) |
|---:|---:|---:|---:|---:|---:|
| 8 | 819,838 | 11,477,732 | 2,174,910 | 2,135,353 | 0.409 |
| 9 | 2,135,353 | 29,894,942 | 4,251,844 | 4,096,606 | 0.946 |
| 10 | 4,096,606 | 57,352,484 | 5,942,914 | 5,479,934 | 1.198 |
| 11 | 5,479,934 | 76,719,076 | 5,709,218 | 4,715,884 | 1.667 |
| 12 | 4,715,884 | 66,022,376 | 3,429,812 | 2,310,917 | 1.039 |

完整数据见 `benchmarks/n14_layers.csv`。单独一次运行以 10 ms 周期采样进程工作集，
得到约 985.9 MiB 峰值；这是外部采样值，不是严格的分配器级峰值。

从 \(N=13\) 到 \(N=14\)：

- 中位时间增加约 6.59 倍；
- 峰值状态增加约 5.60 倍；
- 候选转移增加约 5.97 倍。

因此当前主要瓶颈是稀疏 boundary support、哈希和内存流量，而不是 `u128` 加法。
显式扫描全部 \(N\) 列还产生大量零权重候选：\(N=14\) 共检查 286,010,088 次，
其中只有 23,859,616 次合法，接受率约 8.34%。

## 8. 复现

```powershell
cargo test --release
cargo run --release -- solve 8 --layers
cargo run --release -- bench 14 --min 4 --repeats 3 --csv
```

`solve` 会输出计数、耗时、峰值状态和验证标志；加 `--layers` 可输出逐层 CSV。

## 9. 限制与下一步

本工作只完成用户要求的 naive 小 \(N\) 基线，不能据此声称达到 Issue #34 的正式验收。
当前尚未完成显式 rank-8 局域 \(C\) 张量的独立小网络 oracle，也没有推进
\(Q(16),Q(20),Q(27)\)。

从本次 profiling 出发，最直接的下一步是：

1. 用位运算只枚举可用列，消除约 91.7% 的零权重候选；
2. 为下一层哈希表预估并预留容量，比较更紧凑的 flat hash；
3. 比较批量 sort-reduce，量化状态合并率与峰值临时内存；
4. 增加显式局域张量 truth table / direct-TN oracle，强化与 Sec. VI convention 的独立核验；
5. 再加入对称分片和有限域/CRT，作为推进更大 \(N\) 的严格后端。

