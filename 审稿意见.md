Q: 如果你是审稿人，你觉得这个结果值得发表吗？

A:
## 审稿结论

**有发表价值，但按现在的结果和叙事，我会给“Major Revision / Weak Reject”，不会直接接收为一篇高水平独立研究论文。**

更具体地说：

* 作为黑客松成果、挑战赛技术报告、软件论文或计算方法 workshop 论文：**值得发表，甚至可以较强地接收**。
* 作为计算物理或科学计算方向的完整期刊论文：**有潜力，但需要补关键对照和理论阐释**。
* 作为强调“PEPS 战胜经典 N-Queens 算法”或“推进 Q(28) 计算前沿”的论文：**目前不够**。

## 为什么它确实有论文价值

### 1. 这不是普通的“实现了一个 N 皇后求解器”

报告完成的是一条相当完整的研究链：

* 从显式 rank-9 (B) 和 rank-8 (C) 张量出发；
* 对每一步编译、稀疏化、对称约化和递归重结合做等价性验证；
* 系统检查 D4、decision diagram、future quotient、低秩、不同收缩路径、sort-reduce、内存布局、并行、CRT 和 CPU 热核；
* 对失败方向也保留了机制解释；
* 最终把最初约 25 秒的 (N=14) naive contraction，推进到和独立 DFS comparator 同量级，后来在 (N=14)–18 的特定公平测试批次中快约 21%–25%；
* 同时保留了“explicit (B\to C\to) compiled relation (\to) optimized contraction”的反作弊证据链。

这种系统性和可复现性本身比很多“只展示一个最终算法”的论文更强。

### 2. 它回答了原始论文没有真正回答的问题

Liu、Liao、Wang 的工作主要把 17-nnz 张量网络作为一种“互补的精确枚举路线”提出，并没有给出一套高性能 exact contraction 工程或证明它是否能接近经典搜索。([arXiv][1])

你的工作实际上回答了：

> 这个 PEPS 表示经过充分优化后，究竟会变成什么算法？瓶颈在哪里？哪些标准张量网络技术有效，哪些无效？

这比单纯复现 (Q(N)) 更有研究意义。

### 3. 最有价值的结果是“表示—算法等价性”，而不是那 20% 的 speedup

真正有潜力形成论文核心的发现是：

> Sec. VI 的 17-entry 局域张量，在 row-(v_1) 边界和精确 partial evaluation 下，会机械地编译成一个稀疏有限状态自动机；进一步改变收缩结合顺序、避免晚层 frontier materialization 后，其热循环自然趋近 bitmask DFS。

这解释了很多现象：

* E1、E6 的局域 partial evaluation 有效；
* flat frontier 最终受 record materialization、排序和归并限制；
* DD、低秩和 future quotient 的事后压缩“发现结构但太迟”；
* E38/E40 的 recursive tail 是结构转折点；
* full D4 由于中间 cut 的 stabilizer 很小，额外收益有限；
* 最终 TN contraction 和 DFS 处理的 accepted transitions 数几乎相同。

这是一个很好的“张量网络算法何时退化或编译成组合搜索”的案例研究。通用张量网络用于组合计数本身已有成熟工作，因此论文的新意不能只是“把 CSP 写成张量网络”，而必须放在这种编译、结合顺序和中间表示机制上。([SIAM Ebooks][2])

## 我作为审稿人最大的质疑

### 1. 最终算法到底还是不是一个新的 PEPS 算法？

这是决定文章能否发表的核心问题。

报告已经诚实承认，最终热循环包含：

```text
available = !(columns | diag_dr | diag_dl)
```

并使用 low-bit extraction、shift 和递归。这和 Richards 风格 bitboard DFS 的机器操作高度相似。

你目前的反作弊证据能够说明：

* 它不是调用 DFS 模块；
* 不是查表返回 (Q(N))；
* recurrence 是从显式 (C) fail-closed 编译出来的；
* 每个 reachable parent 都能与 sitewise explicit-(C) contraction 对齐。

这在工程正确性上是充分的。

但从论文创新性看，审稿人仍会问：

> 你是提出了一种新的 tensor-network contraction，还是用张量语言重新推导了已知 DFS？

若文章把结论写成“PEPS 比 DFS 快”，我会比较怀疑；若写成：

> 我们证明并实证展示了一个 exact tensor network 经局域编译和 contraction reassociation 后如何化为搜索型动态程序，并系统刻画这一转化何时发生。

那么它会变成一个更有学术价值的结果。

最好给出一个正式命题或定理：

> 对 Sec. VI 张量网络，在给定边界和逐行顺序下，经过消去行内 virtual indices 后，其 transfer relation 与三 bitmask 的合法位置递推严格同构。

并明确区分：

1. 张量网络定义；
2. contraction tree；
3. 编译后的 automaton；
4. conventional DFS execution schedule。

目前这些内容主要以测试和审计存在，还没有提升为论文级的数学陈述。

### 2. “超过 DFS”对照目前不够公平

这是当前最严重的实验设计问题。

报告自己指出，E47 的 last-6 terminal supernode 是 PEPS 路径的重要收益，而 comparator 没有做相同的 last-6 展开。

因此现在证明的是：

> 经过深入优化的 compiled-C solver，超过了仓库中的一个良好但不是同等优化程度的 DFS comparator。

尚不能证明：

> tensor-network formulation 本身给出了比 bitboard DFS 更强的算法。

我会要求至少增加以下 matched baseline：

* DFS 同样使用 last-4/5/6 unrolling；
* 同样的首行反射约化；
* 同样的任务拆分、线程池和 checked arithmetic 策略；
* 相同的编译参数和代码生成方式；
* 最好把共同的 terminal kernel 抽出来，分别由 TN 编译器和手写 DFS 前端调用。

如果 matched DFS 重新领先，那么论文仍然可以发表，但不能以“PEPS beats DFS”为标题；核心应改成“PEPS compilation recovers a competitive search kernel”。

如果 TN-derived kernel 在严格 matched baseline 下仍能稳定领先，结果会明显更强。

### 3. 没有推进 N-Queens 的计算前沿

原挑战要求至少复现 (Q(27))，headline 目标是未知的 (Q(28))。([GitHub][3])

当前报告最大完整验证值是 (Q(19))，而公开的 GPU/FPGA 路线已经到 (Q(27))；2025 年的 GPU 工作还报告了用八张 RTX 5090 在约 28.4 天复核 (Q(27))，并估计 (Q(28)) 仍需约 11 个月。([arXiv][4])

因此它不能作为“新的 N-Queens exact-enumeration record”发表。

这不意味着文章没有价值，但定位必须准确：

* **不是计算记录论文；**
* 是 exact tensor contraction 的算法解剖、编译和性能研究。

### 4. 目前过于特例化

已有工作已经把 tensor network 用于一般组合优化解空间计数、#CSP 和 weighted model counting。([SIAM Ebooks][2])

所以审稿人会追问：

> 从 N-Queens 得到的结论能否迁移到其他稀疏确定性约束网络？

现在的 60 个实验很深入，但全部围绕一个特殊张量：

* 16 个 identity pass-through；
* 1 个四通道 (0\to1) occupied entry；
* 非负整数权重；
* 强几何结构；
* 行约束强制恰好一个皇后。

至少需要加入一个额外问题族，例如：

* rook / bishop / toroidal queens；
* 带禁用格点的 N-Queens；
* (k)-queens 或 weighted queens；
* 另一种 line-constraint counting model；
* 小规模 independent-set 或 exact-cover 网络。

不必做得和 N-Queens 一样深入。只要证明：

> “explicit tensor → compiled sparse automaton → merge/tail adaptive contraction”

不是只对这个 17-entry tensor 有效，论文的普适性就会提升很多。

## 结果中哪些值得放进正文

不要按 E1–E60 的时间顺序写成论文。那适合作为研究日志和 supplementary material，不适合作为主叙事。

正文应围绕四个结果：

### A. 精确张量到稀疏自动机的机械编译

包括 B/C truth table、fail-closed compiler、边界条件和逐 parent 等价验证。

### B. flat frontier 的瓶颈分解

用 E1、E3、E6、E9、E12、E24、E26、E28、E31、E36 展示：

* 局域扫描；
* bytes/state；
* HashMap 与 sort-reduce；
* candidate materialization；
* 对称性；
* record traffic。

不需要正文逐个介绍所有小优化。

### C. contraction association 的结构转折

E38–E47 是论文最重要的算法段：

* 前若干行 sparse merged prefix；
* 后若干行 recursive tensor tail；
* adaptive cut；
* terminal supernode；
* arithmetic backend；
* 避免晚层物化。

这是从“张量边界算法”走向“搜索型张量收缩”的关键。

### D. 失败机制图谱

选择最有信息量的负结果：

* generic site-level path search 为什么败给 row macro；
* DD 为什么 nodes/support 大于 1；
* 低 rank 为什么因 fill-in 无法直接变成生产收益；
* full D4 为什么受 cut stabilizer 限制；
* memoization 为什么因 suffix 汇合率低而失败。

其余几十个 cache 和 layout 消融放补充材料。

## 我会给出的评分

| 项目    |   当前评分 | 说明                            |
| ----- | -----: | ----------------------------- |
| 技术正确性 | 8.5/10 | exactness、测试、审计和消融非常扎实        |
| 可复现性  |   9/10 | raw CSV、分支、gate 和失败记录优于多数论文   |
| 创新性   |   6/10 | 有新的系统性结论，但容易被视为重新推导 DFS       |
| 结果重要性 | 5.5/10 | 没推进 (Q(N)) 前沿，当前只到 (Q(19))    |
| 方法普适性 | 4.5/10 | 目前基本只有一个特殊张量网络                |
| 实验公平性 |   6/10 | comparator 良好，但 last-6 等优化不对称 |
| 论文成熟度 |   6/10 | 研究材料很丰富，尚缺凝练的中心命题             |

**总体：6.5/10，Weak Reject / Major Revision。**

## 达到“我会接收”的最低修改条件

我认为不必再做 60 个方向。最有效的是四件事：

1. **给出正式的等价性定理。**
   明确证明 row-wise exact contraction 如何编译为三 mask automaton，以及 recursive tail 为什么仍是 contraction-tree reassociation。

2. **补完全 matched DFS baseline。**
   给 DFS 加同样的 last-6、任务拆分和 arithmetic fast path。去掉“优化不对称”这一最大攻击点。

3. **加入至少一个额外约束计数问题。**
   验证编译与 adaptive merge/tail 方法的普适性。

4. **重写贡献表述。**
   不把“60 个优化”和“PEPS 快 25%”当主贡献，而把主结论写成：

   > 精确稀疏张量网络的最佳收缩可能通过 partial evaluation 和 reassociation 自动转化为搜索型算法；我们给出一个可验证的编译链、系统瓶颈分析和 N-Queens 案例。

完成这四项后，我会倾向于 **Weak Accept 到 Accept**，尤其适合计算物理、科学计算、张量网络算法或高性能组合计数方向的期刊。

我最直白的评价是：

> **现在的成果已经超过“黑客松项目”，够资格写论文；但目前最值得发表的不是“我们用 PEPS 算 N 皇后并超过 DFS”，而是“我们揭示并验证了 exact tensor contraction 如何编译成竞争性的组合搜索，以及常见 TN 优化为何成功或失败”。**

[1]: https://arxiv.org/abs/2605.10326 "[2605.10326] Statistical mechanics of the $N$-queens problem"
[2]: https://epubs.siam.org/doi/10.1137/22M1501787?utm_source=chatgpt.com "Computing Solution Space Properties of Combinatorial ..."
[3]: https://github.com/QuantumBFS/quantum.harness/issues/34 "[challenge]: Exact contraction of the tensor network representation of the N-queens problem · Issue #34 · QuantumBFS/quantum.harness · GitHub"
[4]: https://arxiv.org/abs/2511.12009 "High-Performance N-Queens Solver on GPU: Iterative DFS with Zero Bank Conflicts"
