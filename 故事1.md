Q:
你觉得这套研究有哪些结论能够对强关联peps领域有所启示

A:
有，而且我认为其中有几条对强关联 PEPS **相当有启发性**。不过应当区分两层：

* **直接结论**：只对这个 17-nnz、非负整数、精确收缩的 N-Queens PEPS 成立；
* **方法论启示**：可以迁移到 Hubbard、Heisenberg、受挫磁体、费米 iPEPS 等强关联计算，但需要重新验证。

最重要的启示不是“某个排序快了多少”，而是：

> **PEPS 收缩的真实性能主要由中间边界表示及其演化方式决定，而不是由局域张量大小、理论 FLOP 或某个底层 kernel 单独决定。**

---

## 一、边界表示本身就是算法

这套研究最清楚的结论是：同一个局域张量网络，换一种边界表示和 contraction association，算法性质会完全不同。

前期 flat frontier 方法显式保存数百万乃至上千万个 boundary states，需要：

[
\text{生成候选}\rightarrow\text{写入记录}\rightarrow
\text{排序}\rightarrow\text{归并}.
]

后期 E38–E47 改成：

[
\text{小规模 prefix 边界}
\rightarrow
\text{直接递归收缩剩余局域张量},
]

不再物化最宽的晚层 frontier，性能才发生数量级转折。最终被优化掉的不是局域张量运算，而是**错误的中间表示**。

### 对强关联 PEPS 的对应

在普通 PEPS 中，类似的决定性对象是：

* boundary MPS；
* CTMRG 的 corner/edge tensors；
* transfer matrix 的主导子空间；
* 环境 bond dimension (\chi)；
* 单层还是双层表示；
* 吸收、截断和规范化的顺序。

CTMRG 中的 (\chi) 正是环境近似精度和计算成本的核心控制参数。([arXiv][1])

因此一个直接启示是：

> 不要只问“怎样更快地收缩当前环境张量”，而要问“当前环境张量是否应该被显式构造出来”。

这与近年来 split-CTMRG、bra/ket 分层收缩的思路高度一致：通过保持单层或分离层结构，避免过早形成维数为 (D^2) 的双层对象，可以显著降低复杂度，同时维持相近精度。([arXiv][2])

---

## 二、压缩必须发生在 apply 过程中，而不能事后发现“它其实低秩”

这是我认为最值得强关联 PEPS 领域重视的一条。

你的实验中：

* E14 发现 exact future-equivalence classes 只有 concrete states 的约 10%–16%；
* E15 发现有限域 rank 只有 support 的约 4%–30%；
* 说明边界确实存在很强的可压缩结构。

但是这些方法都没有形成 production speedup，因为它们的执行顺序是：

[
\text{先生成完整边界}
\rightarrow
\text{再计算 quotient/rank}.
]

在发现低秩之前，最昂贵的 full support 已经生成了。后面的压缩只能减少存储或 replay 成本，不能收回之前的开销。

### 对物理 PEPS 的意义

这正对应一个常见问题：

> 一个 enlarged boundary tensor 的奇异值衰减很快，不代表“先完整构造它、再 SVD”是高效算法。

真正有效的算法需要在吸收局域张量时就维持低秩结构，例如：

* split-layer contraction；
* factorized environment；
* 增量 QR/SVD；
* matrix-free projector；
* randomized range finding；
* Krylov 型主导子空间更新；
* 直接在 MPS/MPO 表示上 apply，而不是先形成大矩阵。

所以应当区分：

1. **边界事后具有低秩；**
2. **边界可以在不展开高秩对象的情况下被生成。**

第一点只是诊断，第二点才是算法。

这也解释了为什么“测到很漂亮的 entanglement spectrum”并不自动意味着当前 contraction 实现是高效的。

---

## 三、应当按照“未来作用”压缩边界，而不只是按照当前范数压缩

E14/E35 的 future-equivalence 很有概念价值。它问的不是两个 boundary states 当前是否相似，而是：

> 它们对所有剩余张量的作用是否完全相同？

即若两个边界 (b_1,b_2) 对任意剩余网络 (R) 都满足相同的 continuation：

[
\mathcal C(b_1,R)=\mathcal C(b_2,R),
]

那么它们可以被识别为同一个状态。

### 强关联 PEPS 中的类比

普通 boundary MPS/CTMRG 的截断通常依赖：

* Schmidt values；
* reduced density matrix；
* corner spectrum；
* 局域范数最优；
* 某种 Frobenius 范数误差。

但对计算观测量而言，最重要的未必是“当前边界向量的范数误差最小”，而是：

> 该边界误差经过未来 transfer operators 传播后，对最终能量、关联函数或梯度造成多大影响？

这提示一种更强的 **transfer-aware truncation**：

[
\text{截断准则}
\sim
\text{当前权重}
\times
\text{未来传播敏感度}.
]

它与控制论中的 observability/controllability、非厄米矩阵的左右主导子空间，以及 DMRG 中“环境决定最优截断”的思想有相似之处。

对于强关联 PEPS，可以研究：

* 用 transfer matrix 的左、右环境共同定义截断 metric；
* 针对目标观测量构造 observable-aware environment；
* 针对梯度计算构造 adjoint-aware truncation；
* 不同关联长度方向使用不同的 sector-dependent (\chi)；
* 用 approximate future-equivalence，而不是单纯按奇异值大小截断。

PEPS 的边界理论本来就把二维 bulk 与一维 boundary operator 联系起来；临界性、拓扑序和边界相互作用结构都会体现在 boundary state 中。([arXiv][3])

因此你的 future-equivalence 结果可以被理解为：

> 应当寻找“对剩余 transfer problem 不可区分”的最小环境，而不只是低范数误差的环境。

这可能是最具有理论研究潜力的迁移方向。

---

## 四、收缩路径的成本函数必须包含实际压缩行为

E8、E13、E19、E29、E30 和 E34 反复表明：

* dense treewidth；
* open-leg 数量；
* 理论 FLOP；
* 几何波前宽度；
* generic site-level contraction tree；

都可能给出错误判断。

例如 diagonal/corner 路径在几何上看起来更“二维对称”，但实际同时切穿更多约束线，使 sparse support 更大；site-level greedy tree 也远逊于经过整行 partial evaluation 的 row macro。

### 对强关联 PEPS 的启示

对 approximate PEPS，path score 不应只是：

[
\text{cost}\sim \prod_i d_i
]

或最大 dense intermediate size。

更现实的目标函数应包括：

[
\begin{aligned}
\text{score}={}&
w_1,T_{\mathrm{measured}}
+w_2,M_{\mathrm{peak}}
+w_3,\epsilon_{\mathrm{trunc}}\
&+w_4,N_{\mathrm{blocks}}
+w_5,\text{decomposition cost}
+w_6,\text{communication}.
\end{aligned}
]

还应考虑：

* 实际奇异值谱；
* truncation 后的有效 (\chi)；
* 各量子数 sector 的尺寸分布；
* QR/SVD 占比；
* GPU 上 contraction 与 decomposition 的相对速度；
* tensor 是否已经有 factorized structure；
* 下一步是否会立刻截断该 bond。

已有 hyperoptimized approximate contraction 也强调，应同时优化 contraction strategy 和 compression strategy，而不是只优化裸 contraction tree。([arXiv][4])

### 对 TreeSA 的具体启示

强关联 PEPS 中的 TreeSA 不应以“单个 site tensor”为唯一原子，而应允许：

* 完整 row/column transfer MPO；
* bra/ket 分层 absorption；
* corner move；
* half-row transfer tensor；
* local patch；
* QR+SVD truncation；
* symmetry-sector fusion；
* single-layer/double-layer switching；

作为 macro edge。

否则搜索可能只是在一个错误的表示空间里优化。

---

## 五、局域张量应被“编译”，而不只是被通用 einsum 反复解释

E1、E6、E11、E38、E40 和 E47 形成了一条很清楚的链：

1. 从显式 17-entry (C) 建索引；
2. 编译完整 row operator；
3. 从 occupied entry 推导合法位置 iterator；
4. 编译 recursive successor；
5. 生成 certified terminal supernode。

每一步仍然与原始 (C) 逐项等价，但逐渐删除了通用张量解释层的成本。

### 强关联 PEPS 中可以对应为

对于平移不变 iPEPS，同一个 unit-cell tensor 会被吸收很多次。可以考虑为固定的：

* (D)；
* 物理维度 (d)；
* 对称性 sector；
* double-layer 结构；
* CTMRG move；
* unit-cell geometry；

生成专门的 contraction plan 或 kernel，而不是每次交给通用 einsum planner。

例如可以预编译：

* leg fusion 和 permutation；
* symmetry block matching；
* 零块跳过规则；
* QR/SVD reshape；
* bra/ket 共轭关系；
* Hermitian block；
* fermionic swap/sign pattern；
* 常用 corner absorption sequence。

这不意味着手写所有 kernel，而是建立类似编译器的中间表示：

[
\text{PEPS graph}
\rightarrow
\text{symmetry-aware IR}
\rightarrow
\text{factorized contraction IR}
\rightarrow
\text{backend kernel}.
]

你的结果说明，通用张量表达的“语义正确性”与高效执行之间可能隔着非常厚的一层编译。

---

## 六、对称性收益是 cut-dependent 的，不能按照群阶估计

报告中完整 D4 群有 8 个元素，但对 interior row cut，真正保持该 cut 的 stabilizer 通常只有：

[
{e,\sigma_v}.
]

因此首行左右反射可以稳定获得接近 (2\times) 的工作量下降，而完整 D4 在递归 sector 上只能额外减少约 1%–2%，canonicalization 成本反而可能使运行变慢。

### 对强关联 PEPS 的重要区分

必须区分两种对称性：

#### 内部对称性

如：

* (U(1)) 粒子数；
* (SU(2)) 自旋；
* (\mathbb Z_2)；
* fermion parity。

它们直接使 tensor block-sparse，通常对每个中间 cut 都保持，收益往往稳定。

#### 空间对称性

如：

* (C_4)、(C_{4v})；
* reflection；
* translation；
* lattice rotation。

它们是否可用，取决于：

* unit cell 是否保持该对称性；
* contraction cut 是否被该群元素保持；
* 插入观测量后是否仍保持；
* 自发对称破缺是否发生；
* canonicalization 开销是否低于减少的工作量。

因此不能看到 (C_{4v}) 就预期 (8\times) 加速。

物理 PEPS 中采用内部对称性的 block-sparse 表示确实可以大幅节约资源；例如对称张量实现可获得显著内存收益，但 sector 数量很大时，block 管理成本也会成为瓶颈。([arXiv][5])

你的实验给出的进一步提醒是：

> 应当测量每个 contraction cut 上的实际 stabilizer、orbit size 和 block 分布，而不是只记录全局对称群。

---

## 七、block sparsity 之后，元数据和小块调度可能成为新瓶颈

E24、E26、E28、E31、E32 和 E36 的经验很适合对称 PEPS 软件：

* 稀疏 iterator 删除了大量无效检查；
* key sharding 改善 cache locality；
* entry 从 32 byte 降到 24、16、8 byte 后持续有收益；
* arena reuse 减少反复分配；
* bounded flush 比生成完整 thread-local output 更好；
* 但 support 不变时，这些都主要是常数改善。



### 对对称 PEPS 的具体启示

使用 (U(1))、(SU(2)) 后，理论 FLOP 下降不代表代码自然变快。可能出现：

* 数千甚至上万个小 block；
* block metadata 和哈希查找占比上升；
* 小 GEMM 无法充分利用 GPU；
* sector fusion/permutation 比乘法更贵；
* allocator 与临时 tensor 主导；
* 不规则 block size 导致负载不均衡。

已有对称 PEPS 实现也观察到，虽然 (U(1)\times U(1)) 可带来很大的内存节省，但一次操作涉及的 block 数可以超过一万。([arXiv][6])

因此强关联 PEPS 库值得考虑：

* 按 charge tuple 预分 shard；
* 将相同 shape 的小 block 批处理；
* block metadata 使用紧凑 SoA；
* arena/workspace 跨 CTMRG iteration 复用；
* 针对 sector 分布做 load balancing；
* 小 block 聚合为 batched GEMM；
* 直接生成合法 block transition，避免先枚举再判零。

这与报告中的“sparse storage 不等于 sparse transition generation”完全对应。

---

## 八、混合算法往往优于从头到尾坚持一种表示

E40 最终不是选择纯 sort-reduce，也不是从第一行起纯递归，而是根据实际 prefix support 选择切换点：

[
\text{sparse merged prefix}
\quad\longrightarrow\quad
\text{recursive tail}.
]

早期 merge 可以压掉大量重复边界；晚期继续 merge 的物化成本过大，此时递归更合适。

### 强关联 PEPS 的对应研究方向

可以探索自适应切换：

* single-layer (\leftrightarrow) double-layer；
* boundary MPS (\leftrightarrow) CTMRG；
* exact local patch (\leftrightarrow) compressed far environment；
* dense block (\leftrightarrow) symmetry-sparse block；
* direct SVD (\leftrightarrow) iterative eigensolver；
* full update (\leftrightarrow) reduced environment update；
* fixed (\chi) (\leftrightarrow) sector-adaptive (\chi_q)。

切换依据可以是：

* 当前 singular spectrum；
* discarded weight；
* correlation length；
* sector entropy；
* predicted peak memory；
* decomposition/contraction 时间比；
* environment fixed-point residual。

这比“全程只用一个手工规定的 CTMRG move”更有潜力。

---

## 九、热点占比极高，不等于存在同等比例的可删除开销

报告发现最终 99.98% 的时间落在一个 tail kernel 上，但 E51–E60 对 cache、任务布局、suffix cache、inline、last-7/8、overflow、scalar ILP 等优化几乎都没有稳定超过 5%。

原因是该函数名包含的主要是**必须执行的指数级状态转移**，并不是一个多余 wrapper。

### 对 PEPS 性能分析的提醒

在 CTMRG 中，profile 可能显示：

* 90% 在 SVD；
* 95% 在一个 einsum；
* 99% 在某个 eigensolver。

这并不自动意味着“优化该 kernel 可以获得巨大总体收益”。

应进一步区分：

[
\text{必要算术}
\quad\text{vs.}\quad
\text{表示造成的额外算术}.
]

如果 decomposition size 本身由错误的 intermediate representation 导致，那么换更快的 SVD 库可能只带来有限常数收益；改变吸收和截断顺序才可能降低矩阵尺寸。

这也是为什么 split-CTMRG 或 sequential bra/ket contraction 可能比单纯加速原有 decomposition 更有意义。CTMRG 的 projector/decomposition 往往是主要瓶颈，而近期方法正尝试通过改变表示和分解方案降低这一成本。([arXiv][7])

---

## 十、最值得进一步迁移的三个研究方向

### 1. 面向未来传播的环境截断

研究目标：

> 构造一个 environment basis，使其不仅重构当前边界准确，而且对后续 transfer operators 和目标观测量具有最小误差。

可以比较：

* 普通 density-matrix/SVD truncation；
* 左右 transfer fixed point metric；
* observable-aware truncation；
* gradient-aware truncation；
* approximate future-equivalence。

这是 E14/E35 最深层的物理迁移。

### 2. 以实际 (\chi)、sector 和误差为成本的路径搜索

把 TreeSA/greedy 搜索对象改成 PEPS macro operations，cost 由真实小规模试运行标定：

[
(\text{wall time},\text{RSS},
\epsilon_{\rm trunc},
\chi_{\rm eff},
\text{sector histogram}).
]

然后在小 (D,\chi) 上搜索，冻结 motif，再迁移到大 (D,\chi)，避免搜索成本混入 production。

这吸收了 E13/E30 的失败教训，也与 approximate contraction 的 hyperoptimization 方向一致。([arXiv][8])

### 3. 避免显式形成双层大对象的 online factorization

优先研究：

* split-CTMRG；
* single-layer environment；
* bra/ket sequential contraction；
* factorized corner tensors；
* matrix-free projector construction；
* sector-aware incremental QR；
* 只在必要位置形成局部 double layer。

这是最直接、最可能短期产生工程收益的迁移。

---

## 哪些结论不能直接迁移

必须保持谨慎。N-Queens 张量与强关联 PEPS 差异很大：

| N-Queens PEPS           | 强关联物理 PEPS                             |
| ----------------------- | -------------------------------------- |
| 17 个非零元                 | 通常局域 tensor 较稠密                        |
| 非负整数                    | 实数/复数，可能有符号抵消                          |
| 精确收缩                    | 必须近似截断                                 |
| Boolean virtual legs    | bond dimension (D) 可很大                 |
| exact support           | numerical rank / entanglement spectrum |
| 无 gauge conditioning 问题 | gauge、norm matrix、病态性很重要               |
| 固定组合计数                  | tensor 还需反复优化和求梯度                      |
| 清晰的 automaton           | 未必能编译成简单有限状态递推                         |

因此不能根据 DD、低秩或 full D4 在 N-Queens 中失败，就断言它们在物理 PEPS 中也失败。

尤其是：

* N-Queens 的“support”不等于物理 PEPS 的 (\chi)；
* exact future class 不等于 Schmidt state；
* 组合问题中的零元素稀疏性不等于量子态的低纠缠；
* 递归尾收缩类似 DFS 的成功，依赖这个局域张量可编译成极简单的位运算自动机。

---

## 总结

我认为对强关联 PEPS 最重要的五条启示是：

1. **边界表示和 contraction association 比局域 kernel 更重要。**
2. **低秩或可压缩性必须在 apply 过程中保持，事后压缩往往太迟。**
3. **路径搜索必须以实际截断后维度、sector 分布、误差和 wall time 为成本。**
4. **对称性收益取决于当前 cut 的 stabilizer 和 block 结构，不能按群阶估计。**
5. **最有潜力的新方向是 transfer-aware、future-aware 的环境压缩，而不是继续优化同一个 dense contraction。**

其中最具理论价值的是第三种表述：

> **PEPS 环境应被视为对剩余二维网络的“预测状态”。最优压缩应保留未来可观测作用，而不仅是当前边界范数。**

这比某个特定 N-Queens 加速更可能形成对强关联 PEPS 有长期价值的研究方向。

[1]: https://arxiv.org/html/2308.12358v3?utm_source=chatgpt.com "An introduction to infinite projected entangled-pair state ..."
[2]: https://arxiv.org/pdf/2502.10298?utm_source=chatgpt.com "arXiv:2502.10298v2 [cond-mat.str-el] 26 Apr 2025"
[3]: https://arxiv.org/abs/1103.3427?utm_source=chatgpt.com "Entanglement spectrum and boundary theories with projected entangled-pair states"
[4]: https://arxiv.org/abs/2206.07044?utm_source=chatgpt.com "Hyper-optimized approximate contraction of tensor networks with arbitrary geometry"
[5]: https://arxiv.org/html/2405.12196v1?utm_source=chatgpt.com "YASTN: Yet another symmetric tensor networks; A Python ..."
[6]: https://arxiv.org/pdf/2405.12196?utm_source=chatgpt.com "arXiv:2405.12196v1 [cond-mat.str-el] 20 May 2024"
[7]: https://arxiv.org/pdf/2503.13900?utm_source=chatgpt.com "Ace-TN: GPU-Accelerated Corner-Transfer-Matrix ..."
[8]: https://arxiv.org/abs/2002.01935?utm_source=chatgpt.com "Hyper-optimized tensor network contraction"

Q:
自旋冰问题是否可以仿照N皇后问题写出一个PEPS，然后和包含了所有自旋可能的直积态求内积？这样做有什么对应的物理意义吗？或者说，这个N皇后问题的组合计数情景，有什么可以迁移到物理情景

A:
可以，而且**自旋冰比 N 皇后更天然地适合这种构造**。N 皇后的行、列、对角线约束原本是非局域的，需要用虚指标把“是否已经遇到皇后”的信息沿整条约束线传播；自旋冰的 ice rule 本身就是局域顶点或四面体约束，可以直接写成一个只有少数非零元的局域张量。

不过需要区分三件事：

1. 用张量网络计算经典自旋冰的合法构型数；
2. 把合法构型的等权叠加写成一个 PEPS 量子态；
3. 研究真正含量子跃迁的 quantum spin ice。

前两者与你说的构造完全对应，第三者还需要加入非对角动力学。

---

## 1. 自旋冰的局域约束张量

以 square ice 或 pyrochlore spin ice 的对偶 diamond lattice 表示为例，把 Ising 自旋看成定义在边上的箭头：

[
\sigma_e=\pm1.
]

对每个四配位顶点 (v)，定义带方向的“磁荷”

[
Q_v=\sum_{e\ni v}\eta_{ve}\sigma_e,
]

其中 (\eta_{ve}=\pm1) 表示该边相对于顶点是流入还是流出。ice rule 是

[
Q_v=0,
]

也就是 two-in/two-out。

于是可以定义局域四阶张量

[
T_v(\sigma_1,\sigma_2,\sigma_3,\sigma_4)
========================================

\begin{cases}
1,&Q_v=0,\
0,&Q_v\neq0.
\end{cases}
]

四个二值指标共有 (2^4=16) 种组合，其中恰好 6 种满足 two-in/two-out，因此这是一个有 6 个非零元的局域约束张量。冰规则确实可以这样直接编码；相关张量网络工作就是用四腿、bond dimension 2 的局域张量表示“恰好两个近、两个远”或 two-in/two-out，并通过收缩计算残余熵。([arXiv][1])

全局振幅是

[
\Psi_{\rm ice}(\boldsymbol{\sigma})
===================================

\prod_v
T_v\bigl({\sigma_e:e\ni v}\bigr).
]

因此

[
\Psi_{\rm ice}(\boldsymbol{\sigma})
===================================

\begin{cases}
1,&\boldsymbol{\sigma}\text{ 满足所有 ice rules},\
0,&\text{否则}.
\end{cases}
]

这和 N 皇后 PEPS 的逻辑完全一致：局域张量网络是一个全局约束的 indicator function。

---

## 2. 怎样把它写成带物理指标的 PEPS

自旋变量位于边上，而一个边自旋同时参与两端顶点的约束。可以在每条边上放一个 copy tensor：

[
C^{s}_{ab}
==========

\delta_{s,a}\delta_{s,b},
]

其中：

* (s) 是物理自旋；
* (a,b) 分别送往边两端的两个 ice-rule 张量；
* (a,b,s\in{\uparrow,\downarrow})。

整个状态就是

[
|\Psi_{\rm ice}\rangle
======================

\sum_{\boldsymbol{\sigma}}
\left[
\prod_v T_v(\boldsymbol{\sigma}_{\partial v})
\right]
|\boldsymbol{\sigma}\rangle
===========================

\sum_{\boldsymbol{\sigma}\in\mathcal I}
|\boldsymbol{\sigma}\rangle,
]

其中 (\mathcal I) 是所有合法冰构型。

在二维 square ice 中，这可以看成 bond dimension 2 的 decorated PEPS；通过适当 blocking，也可以整理成标准二维 PEPS。

对于三维 pyrochlore spin ice，需要稍作修正：

* pyrochlore 自旋可以看成位于对偶 diamond lattice 的边上；
* 每个 pyrochlore 四面体对应 diamond lattice 的一个顶点；
* two-in/two-out 就是该顶点上的局域约束；
* 所得对象本质上是一个**三维张量网络**，不再是通常意义上的二维 PEPS。

实际数值计算可以沿一个方向构造 layer-to-layer transfer operator，并用二维 PEPS 表示或近似其主导边界态。已有工作正是利用二维 PEPS 方法计算三维冰与其他受挫经典体系的自由能、残余熵和关联函数。([arXiv][2])

对于四面体或 simplex 几何，PESS 有时比普通 pair-based PEPS 更自然，因为约束本身作用在整个四面体上，而不是一对自旋上。PESS 正是把 entangled pair 推广到 simplex 的张量网络态。([arXiv][3])

---

## 3. 与“包含所有自旋可能的直积态”求内积

定义未归一化的全配置直积态

[
|\Sigma\rangle
==============

\bigotimes_{e}
\left(
|\uparrow\rangle_e+|\downarrow\rangle_e
\right)
=======

\sum_{\boldsymbol{\sigma}}
|\boldsymbol{\sigma}\rangle.
]

那么

[
\langle\Sigma|\Psi_{\rm ice}\rangle
===================================

\sum_{\boldsymbol{\sigma}}
\Psi_{\rm ice}(\boldsymbol{\sigma})
===================================

\Omega_{\rm ice},
]

其中 (\Omega_{\rm ice}) 是满足所有 ice rule 的构型总数。

这正是 N 皇后中把每个 physical leg 与 ((1,1)) 收缩的对应操作：

[
\text{对每个物理变量求和}
\quad\Longrightarrow\quad
\text{统计所有合法全局配置}.
]

不过在自旋冰的 0/1 等权构造中，还有一个更直接的关系：

[
\langle\Psi_{\rm ice}|\Psi_{\rm ice}\rangle
===========================================

\Omega_{\rm ice}.
]

因为每个合法构型振幅为 1，非法构型振幅为 0，计算范数本身就会计数。因此：

* 与全配置积态求内积：对所有物理配置求和；
* 求 PEPS 范数：对每个合法配置的振幅平方求和；

在这个特殊的 0/1 等权情形下，两者数值相同。

---

## 4. 这个内积的物理意义是什么

### 4.1 经典意义：残余熵

最直接的物理量是零温基态简并度和 residual entropy：

[
S_0=k_{\rm B}\ln\Omega_{\rm ice}.
]

热力学极限下，更关心每个自旋或每个顶点的熵密度：

[
s_0
===

\lim_{N\to\infty}
\frac{k_{\rm B}}{N}\ln\Omega_{\rm ice}.
]

这就是普通水冰和磁性 spin ice 中著名的残余熵来源：大量满足局域 ice rule 的构型在低能量上简并。张量网络方法已经用于直接求三维受挫模型、水冰 (I_h/I_c) 的残余熵，并通过关联函数识别 Coulomb phase。([arXiv][2])

因此这个内积不是纯粹形式操作，而是在计算一个真正的热力学量。

---

### 4.2 归一化后的内积：约束流形在完整 Hilbert 空间中的比例

定义归一化的等权冰态

[
|{\rm Ice}\rangle
=================

\frac{1}{\sqrt{\Omega_{\rm ice}}}
\sum_{\boldsymbol{\sigma}\in\mathcal I}
|\boldsymbol{\sigma}\rangle,
]

以及归一化的全配置积态

[
|+\rangle^{\otimes E}
=====================

\frac{1}{\sqrt{2^E}}
\sum_{\boldsymbol{\sigma}}
|\boldsymbol{\sigma}\rangle.
]

那么

[
\left|
\langle +^{\otimes E}|{\rm Ice}\rangle
\right|^2
=========

\frac{\Omega_{\rm ice}}{2^E}.
]

右边可以解释为：

> 从所有 (2^E) 个独立随机自旋配置中均匀抽取一个，恰好满足全部 ice rule 的概率。

所以这个 fidelity 的指数衰减率直接编码熵密度：

[
\frac{S_0}{k_{\rm B}E}
======================

\ln2+
\frac{1}{E}
\ln
\left|
\langle +^{\otimes E}|{\rm Ice}\rangle
\right|^2.
]

因此“与全自旋积态的重叠”可以理解成局域约束从完整 Hilbert 空间中筛掉了多少配置。

但要注意：这个重叠通常不是实验中最自然的局域可观测量，更像是：

* 全局 fidelity；
* 配置空间体积比例；
* 熵密度的生成量。

---

## 5. 作为量子态，它对应什么

等权叠加态

[
|{\rm Ice}\rangle
=================

\frac1{\sqrt{\Omega_{\rm ice}}}
\sum_{\boldsymbol{\sigma}\in\mathcal I}
|\boldsymbol{\sigma}\rangle
]

是一个纯量子态，而经典自旋冰在零温简并流形上的等权系综通常是混态

[
\rho_{\rm cl}
=============

\frac1{\Omega_{\rm ice}}
\sum_{\boldsymbol{\sigma}\in\mathcal I}
|\boldsymbol{\sigma}\rangle
\langle\boldsymbol{\sigma}|.
]

两者对所有对角算符的期望值相同：

[
\langle {\rm Ice}|\hat O_{\rm diag}|{\rm Ice}\rangle
====================================================

\operatorname{Tr}(\rho_{\rm cl}\hat O_{\rm diag}),
]

但它们的非对角相干、纠缠和动力学完全不同。

这种等权叠加态属于 Rokhsar–Kivelson 型构造。在 quantum dimer、quantum six-vertex 或 quantum spin-ice 模型的特殊 RK 点，基态可以是某个连通拓扑 sector 内所有经典合法构型的等权叠加；相关 square-ice quantum link model 同时具有 Gauss-law 约束和 RK coupling。([arXiv][4])

所以存在一条清楚的对应：

[
\text{经典合法配置空间}
\longrightarrow
\text{RK 等权量子态}.
]

但仅仅构造这个 PEPS 并计数，还没有包含 quantum spin ice 的全部物理。真正的量子自旋冰还需要：

* plaquette/ring-exchange；
* 不同冰构型之间的量子隧穿；
* 非对角矩阵元；
* emergent photon；
* monopole/spinon 动力学。

等权 PEPS 只描述一个特殊波函数，而不是一般 quantum spin-ice Hamiltonian 的完整求解。

---

## 6. 有限温度怎样推广

假设经典能量可以写成局域项

[
H(\boldsymbol{\sigma})
======================

\sum_x h_x(\boldsymbol{\sigma}_{\partial x}),
]

则配分函数是

[
Z(\beta)
========

\sum_{\boldsymbol{\sigma}}
e^{-\beta H(\boldsymbol{\sigma})}.
]

只需把 0/1 约束张量替换为 Boltzmann-weight tensor：

[
T_x(\boldsymbol{\sigma}_{\partial x})
=====================================

e^{-\beta h_x(\boldsymbol{\sigma}_{\partial x})},
]

全网络收缩就得到 (Z)。

也可以定义 coherent Gibbs state：

[
|\psi_\beta\rangle
==================

\sum_{\boldsymbol{\sigma}}
e^{-\beta H(\boldsymbol{\sigma})/2}
|\boldsymbol{\sigma}\rangle.
]

它满足

[
\langle\psi_\beta|\psi_\beta\rangle
===================================

Z(\beta).
]

归一化后，对角关联函数正好等于经典 Gibbs 分布中的热力学关联函数。二维局域经典模型的这类 coherent thermal states 可以表示成 PEPS，并可对应为局域量子 Hamiltonian 的基态。([arXiv][5])

对自旋冰而言，可以加入：

* monopole defect energy；
* 外磁场；
* 交换相互作用；
* dipolar interaction 的近似或辅助变量表示；
* 不同 vertex configuration 的权重。

于是就从“合法构型计数”推广成了真正的经典统计物理配分函数。

---

## 7. N 皇后组合计数能够迁移到哪些物理情景

### A. 基态简并度和残余熵

这是最直接的迁移：

[
\Omega
======

#{\text{满足所有局域低能约束的构型}}.
]

物理例子包括：

* spin ice；
* water ice；
* dimer coverings；
* fully packed loops；
* frustrated Ising ground-state manifolds；
* graph coloring 型自旋模型；
* lattice gauge theory 的 Gauss-law 子空间。

张量网络既可以计算计数，也可以计算 ground-state entropy。对 spin glasses，甚至可以用 tropical tensor algebra 同时求基态能量、最优构型和基态简并度。([arXiv][6])

---

### B. 缺陷和磁单极子的生成函数

把严格 ice tensor

[
T_v=\delta_{Q_v,0}
]

推广为

[
T_v(z)
======

\begin{cases}
1,&Q_v=0,\
z,&Q_v=\pm2,\
z_4,&Q_v=\pm4,
\end{cases}
]

则全收缩给出

[
Z(z,z_4)
========

\sum_{\boldsymbol{\sigma}}
z^{N_2(\boldsymbol{\sigma})}
z_4^{N_4(\boldsymbol{\sigma})}.
]

于是

[
z\frac{\partial\ln Z}{\partial z}
=================================

\langle N_2\rangle
]

给出单极缺陷数，进一步的导数给出涨落和 susceptibility。

在 N 皇后里，这对应于把“违反约束的配置权重设为 0”推广为“按冲突数量赋予 fugacity”。

---

### C. 拓扑 sector 的分辨计数

周期边界下，ice configurations 通常可以按全局 flux 或 winding numbers 分类：

[
\Omega
======

\sum_{\boldsymbol{w}}
\Omega_{\boldsymbol{w}}.
]

通过选择不同的虚边界条件、插入 twist 或 flux projector，可以分别计算：

* 每个 winding sector 的简并度；
* sector free-energy difference；
* flux stiffness；
* topological sector mixing。

这比只计算总构型数更接近 Coulomb phase 和 emergent gauge field 的物理。

---

### D. 关联函数

在两个位置插入自旋因子：

[
\langle\sigma_i\sigma_j\rangle
==============================

\frac{
\sum_{\boldsymbol{\sigma}}
\sigma_i\sigma_j
\prod_vT_v
}{
\sum_{\boldsymbol{\sigma}}
\prod_vT_v
}.
]

张量网络上只需修改对应的 copy tensor 或插入 diagonal operator。

因此从计数网络可以直接得到：

* spin-spin correlation；
* charge-charge correlation；
* structure factor；
* pinch points；
* dipolar Coulomb-phase correlations。

三维冰的张量网络研究已经用这种思路计算出 Coulomb phase 的 dipolar correlation form。([arXiv][2])

---

### E. 约束投影算符

还可以把整个网络理解成一个投影到物理约束子空间的算符：

[
P_{\rm ice}
===========

\prod_v \delta_{Q_v,0}.
]

作用在无约束积态上：

[
P_{\rm ice}|+\rangle^{\otimes E}
================================

\sum_{\boldsymbol{\sigma}\in\mathcal I}
|\boldsymbol{\sigma}\rangle.
]

这正是“先列出全部配置，再用局域约束投影筛选”的量子版本。

在 gauge theory 语言中，ice rule 类似 Gauss law；这个张量网络是在构造 gauge-invariant Hilbert space。量子自旋冰和量子 link model 的约束正可以用这种 Gauss-law 结构表述。([arXiv][4])

---

## 8. 它与 N 皇后最关键的差别

| N 皇后                  | 自旋冰                            |
| --------------------- | ------------------------------ |
| 行、列、对角线是长程约束          | ice rule 是顶点/四面体局域约束           |
| 需要虚指标传播“是否已有皇后”       | 虚指标基本就是边自旋/箭头                  |
| 目标主要是计数 (Q(N))        | 计数对应残余熵和配分函数                   |
| 没有天然热力学局域 Hamiltonian | 有明确的经典局域能量与低能流形                |
| 对称性主要是棋盘 D4           | 有空间对称性、Gauss law 和 flux sector |
| 输出通常是一个整数             | 可输出自由能、熵、关联、缺陷密度、sector 权重     |
| 等权叠加主要是计算构造           | 等权叠加可对应 RK quantum state       |

所以从物理角度，自旋冰不是“勉强套用 N 皇后方法”，而是 N 皇后组合计数框架的一种更标准的统计物理版本。

---

## 9. 一个最适合实际尝试的项目

我建议先从 **二维 square ice** 做，而不是直接从三维 pyrochlore 开始。

可以按以下层次推进：

### 第一步：精确计数

构造六顶点张量

[
T_{udlr}=\delta_{\text{two-in/two-out}},
]

在 (L_x\times L_y) 周期或开放格子上精确收缩，计算

[
\Omega(L_x,L_y).
]

同时按 winding sector 分类。

### 第二步：残余熵

计算

[
s_0(L)
======

\frac{1}{N}\ln\Omega(L),
]

并外推热力学极限。

### 第三步：等权 RK PEPS

显式保留 physical legs，构造

[
|{\rm Ice}\rangle
=================

\sum_{\rm ice}|\sigma\rangle.
]

计算：

* 范数；
* 与 (|+\rangle^{\otimes E}) 的重叠；
* diagonal correlations；
* entanglement entropy；
* 不同 winding sector 的叠加或分离。

### 第四步：加入缺陷和温度

例如

[
H=\Delta\sum_v Q_v^2-h\sum_e\sigma_e,
]

构造 (e^{-\beta H}) 的局域张量网络，研究：

* monopole density；
* specific heat；
* magnetic susceptibility；
* correlation length；
* crossover into/out of ice manifold。

### 第五步：量子动力学

加入 plaquette flip：

[
H_{\rm q}
=========

-t\sum_p
\left(
|\circlearrowright\rangle\langle\circlearrowleft|
+\text{h.c.}
\right)
+
V\sum_p P^{\rm flippable}_p.
]

此时就不再只是组合计数，而是 quantum six-vertex/quantum spin-ice 问题。PEPS 变成真正的量子变分态，而不仅是一个约束指示器。

---

## 核心结论

**可以这样做，而且有明确的物理意义。**

最精确的对应是：

[
\boxed{
\text{局域约束张量}
+
\text{全配置积态}
\quad\Longrightarrow\quad
\text{合法构型数}
}
]

对于自旋冰，这个合法构型数给出：

[
\boxed{
\Omega_{\rm ice}
\longrightarrow
S_0=k_{\rm B}\ln\Omega_{\rm ice}
}
]

而将合法构型做相干等权叠加，则得到：

[
\boxed{
|{\rm Ice}\rangle
=================

\sum_{\rm ice}|\sigma\rangle
}
]

它是一个 Rokhsar–Kivelson 型受约束量子态。继续加入 Boltzmann 权重，可以得到 classical partition function 或 coherent Gibbs PEPS；加入 plaquette/ring-exchange 后，才进入真正的 quantum spin-ice 动力学。

因此，N 皇后组合计数最有价值的物理迁移不是“用 PEPS 解另一个谜题”，而是：

> **把局域约束定义的低能流形写成张量网络，利用收缩计算其熵、配分函数、拓扑 sector、缺陷统计和关联函数，再把该经典流形提升为 RK 型量子态。**

[1]: https://arxiv.org/pdf/2511.22477?utm_source=chatgpt.com "Equivalence of residual entropy of hexagonal and cubic ..."
[2]: https://arxiv.org/abs/1805.10598?utm_source=chatgpt.com "Residual entropies for three-dimensional frustrated spin systems with tensor networks"
[3]: https://arxiv.org/abs/1307.5696?utm_source=chatgpt.com "Tensor renormalization of quantum many-body systems using projected entangled simplex states"
[4]: https://arxiv.org/pdf/1807.00826?utm_source=chatgpt.com "arXiv:1807.00826v2 [cond-mat.stat-mech] 4 Feb 2019"
[5]: https://arxiv.org/pdf/quant-ph/0601075?utm_source=chatgpt.com "arXiv:quant-ph/0601075v2 27 Jan 2006"
[6]: https://arxiv.org/abs/2008.06888?utm_source=chatgpt.com "Tropical Tensor Network for Ground States of Spin Glasses"

Q:
Rydberg blockade呢？是否也可以这么干吗？

A:
可以，而且 **Rydberg blockade 比 N 皇后更直接地对应组合计数**。在硬 blockade 极限下，它本质上就是图上的独立集约束：

> 两个位于 blockade 半径以内的原子不能同时处于 Rydberg 激发态。

这正等价于：在以原子为顶点、blockade 关系为边的图 (G=(V,E)) 上，所有激发顶点组成一个 independent set。Rydberg 阵列用于 maximum independent set 的实验和算法正是基于这个映射。([arXiv][1])

---

# 1. 最简单的 blockade 约束张量

令每个原子的物理变量为

[
n_i\in{0,1},
]

其中：

* (n_i=0)：基态 (|g\rangle)；
* (n_i=1)：Rydberg 激发态 (|r\rangle)。

若 ((i,j)\in E) 表示两个原子处于 blockade 范围内，则硬约束为

[
n_i n_j=0.
]

可以给每条边定义一个二阶约束张量

[
B_{n_i n_j}
===========

# 1-n_i n_j

\begin{pmatrix}
1&1\
1&0
\end{pmatrix}.
]

因此整个配置 (\boldsymbol n) 的指示函数为

[
\Psi_{\rm bl}(\boldsymbol n)
============================

\prod_{(i,j)\in E}
B_{n_i n_j}
===========

\prod_{(i,j)\in E}(1-n_i n_j).
]

于是

[
\Psi_{\rm bl}(\boldsymbol n)
============================

\begin{cases}
1,&\boldsymbol n\text{ 满足 blockade},\
0,&\text{存在相邻的两个激发}.
\end{cases}
]

这个边张量 (\begin{pmatrix}1&1\1&0\end{pmatrix}) 也是张量网络表示 independent-set 问题时最自然的局域对象。([arXiv][2])

---

# 2. 它能否写成 PEPS？

可以。

不过最自然的初始表示是 **factor graph tensor network**：

* 每个原子对应一个物理变量 (n_i)；
* 每条 blockade 边对应一个约束张量 (B_{n_i n_j})；
* 每个物理变量通过 copy tensor 送到所有相邻约束。

对于一个度数为 (z_i) 的顶点，可以写 copy tensor

[
C^{s_i}*{a_1a_2\cdots a*{z_i}}
==============================

\delta_{s_i,a_1}
\delta_{s_i,a_2}\cdots
\delta_{s_i,a_{z_i}}.
]

其中 (s_i) 是物理指标，(a_k) 是通向各 blockade 边的虚指标。

将 copy tensors 与所有边约束 (B) 收缩后，得到

[
|\Psi_{\rm bl}\rangle
=====================

\sum_{\boldsymbol n}
\prod_{(i,j)\in E}(1-n_i n_j)
|\boldsymbol n\rangle
=====================

\sum_{I\in \operatorname{Ind}(G)}
|I\rangle.
]

这里 (\operatorname{Ind}(G)) 表示图 (G) 的全部独立集。

对于规则二维阵列：

* 最近邻 square-lattice blockade 可以整理成标准二维 PEPS；
* 三角、ruby、kagome 等阵列也可以按其几何结构构造；
* 若 blockade 半径覆盖较远邻居，需要增加长程虚边、装饰格点或做 blocking，因此不一定仍是最简单的最近邻 PEPS。

---

# 3. 与“所有自旋可能的积态”求内积

定义未归一化全配置积态

[
|\Sigma\rangle
==============

\bigotimes_{i=1}^{N}
\left(|0\rangle_i+|1\rangle_i\right)
====================================

\sum_{\boldsymbol n}
|\boldsymbol n\rangle.
]

那么

[
\langle\Sigma|\Psi_{\rm bl}\rangle
==================================

\sum_{\boldsymbol n}
\prod_{(i,j)\in E}(1-n_i n_j)
=============================

#\operatorname{Ind}(G).
]

也就是说，这个内积给出：

[
\boxed{\text{满足 Rydberg blockade 的全部经典配置数}}
]

这与 N 皇后完全同构：

| N 皇后          | Rydberg blockade       |
| ------------- | ---------------------- |
| 每个格点是否有皇后     | 每个原子是否被激发              |
| 同行、同列、同对角不能冲突 | blockade 图上的相邻顶点不能同时激发 |
| 合法皇后布局        | 图的 independent set     |
| 与全配置态求内积      | 统计所有 independent sets  |

甚至 Rydberg blockade 更简单，因为约束本身是局域二体约束，不需要像 N 皇后那样沿整行、整列和对角线传播“是否已经出现皇后”的状态。

---

# 4. 这个计数有什么物理意义？

## 4.1 受约束 Hilbert 空间的维数

硬 blockade 下，允许的基矢只有 independent sets，因此约束 Hilbert 空间为

[
\mathcal H_{\rm bl}
===================

\operatorname{span}
\left{
|I\rangle:I\in\operatorname{Ind}(G)
\right}.
]

其维数就是

[
\dim\mathcal H_{\rm bl}
=======================

#\operatorname{Ind}(G).
]

所以这里不只是“组合题答案”，而是在计算：

> 强 blockade 极限下，量子系统实际可访问的基态空间有多大。

在一维最近邻 blockade 链中，这个维数按 Fibonacci 数增长；更一般的二维阵列则对应相应图的 independent-set 计数。PXP 模型正是定义在这种受 blockade 限制、排除相邻激发的 Hilbert 空间中。([arXiv][3])

---

## 4.2 经典 hard-core lattice gas 的配分函数

更一般地，在每个激发上加入 fugacity (z)，定义

[
|\Psi(z)\rangle
===============

\sum_{I\in\operatorname{Ind}(G)}
z^{|I|/2}|I\rangle.
]

它的范数为

[
\langle\Psi(z)|\Psi(z)\rangle
=============================

\sum_{I\in\operatorname{Ind}(G)}
z^{|I|}
\equiv
Z_G(z).
]

(Z_G(z)) 就是图的 independence polynomial，也正是 classical hard-core lattice gas 的 grand partition function：

[
z=e^{\beta\mu}.
]

其中 (\mu) 是粒子的化学势。

因此：

[
\langle N_{\rm exc}\rangle
==========================

z\frac{\partial\ln Z}{\partial z},
]

而

[
z\frac{\partial}{\partial z}
\langle N_{\rm exc}\rangle
]

给出激发数涨落或可压缩率类量。

对于 square lattice 最近邻 blockade，这就是 hard-square model。张量网络和 CTMRG 已被直接用于研究 hard-square 配分函数及其有序相和相变，而该模型也被用作 Rydberg 约束体系的经典对应。([arXiv][4])

---

## 4.3 约束熵

在 (z=1) 时，

[
S_{\rm config}
==============

k_{\rm B}\ln #\operatorname{Ind}(G)
]

是所有 blockade-allowed 配置的构型熵。

热力学极限下可以定义熵密度

[
s_{\rm bl}
==========

\lim_{N\to\infty}
\frac{1}{N}
\ln#\operatorname{Ind}(G).
]

它衡量 blockade 将原始 (2^N) 维 Hilbert 空间压缩了多少。

定义归一化状态

[
|\overline\Psi_{\rm bl}\rangle
==============================

\frac{1}{\sqrt{#\operatorname{Ind}(G)}}
\sum_{I\in\operatorname{Ind}(G)}
|I\rangle,
]

则与全 (+) 积态的重叠为

[
\left|
\langle +|^{\otimes N}
\overline\Psi_{\rm bl}
\rangle
\right|^2
=========

\frac{#\operatorname{Ind}(G)}{2^N}.
]

右边就是：

> 随机独立地把每个原子设成激发或不激发时，配置恰好满足 blockade 的概率。

---

# 5. 与 maximum independent set 的关系

上面的收缩统计的是**所有 independent sets**，而不是只统计最大的 independent sets。

加入激发数权重：

[
Z_G(z)=\sum_{I\in\operatorname{Ind}(G)}z^{|I|}.
]

在 (z\to\infty) 时，最高次幂由最大独立集控制：

[
Z_G(z)
\sim
g_{\rm MIS},z^{\alpha(G)},
]

其中：

* (\alpha(G)) 是 maximum independent set 的大小；
* (g_{\rm MIS}) 是最大独立集的简并度。

所以张量网络可以同时回答：

1. 最大允许激发数是多少；
2. 达到这个最大激发数的配置有多少个；
3. 各激发数 sector 中分别有多少个配置。

也可以把普通数域换成 tropical semiring，直接从张量收缩中提取最大独立集权重。([arXiv][2])

在实际 Rydberg Hamiltonian 中，正的 detuning 通常偏好更多 Rydberg 激发，而强 blockade 禁止相邻激发，因此低能问题自然对应 maximum independent set 或 maximum-weight independent set。该映射已用于二维 Rydberg 阵列上的组合优化实验。([arXiv][1])

---

# 6. 这个等权 PEPS 是一个真正的量子态吗？

是。

[
|\overline\Psi_{\rm bl}\rangle
==============================

\frac1{\sqrt{\mathcal N}}
\sum_{I\in\operatorname{Ind}(G)}|I\rangle
]

是所有 blockade-allowed 配置的相干等权叠加。

它与经典混态

[
\rho_{\rm cl}
=============

\frac1{\mathcal N}
\sum_{I\in\operatorname{Ind}(G)}
|I\rangle\langle I|
]

不同：

* 对所有 diagonal observables，两者结果相同；
* 但纯态包含独立集之间的量子相干和纠缠；
* 非对角关联函数会不同。

更一般的

[
|\Psi(z)\rangle
===============

\frac1{\sqrt{Z_G(z)}}
\sum_I z^{|I|/2}|I\rangle
]

是典型的 Rokhsar–Kivelson 型受约束波函数。对于一维 Rydberg blockade 模型，确实存在 frustration-free 参数点，其精确基态可以写成这种带 fugacity 权重的受约束叠加，并具有精确 MPS 表示。([arXiv][5])

因此这个 PEPS 并不只是计数工具，也可以作为：

* Rydberg 受约束量子态；
* 变分波函数；
* RK 型 parent Hamiltonian 的精确基态；
* PXP/Rydberg Hamiltonian 的初始 ansatz。

---

# 7. 但它不是一般 Rydberg 系统的完整解

实际常见的 Rydberg Hamiltonian 是

[
H
=

\frac{\Omega}{2}\sum_i \sigma_i^x
-\Delta\sum_i n_i
+
\sum_{i<j}V_{ij}n_i n_j.
]

在强最近邻 blockade 极限下，可以投影到约束空间，得到类似

[
H_{\rm eff}
===========

\frac{\Omega}{2}
\sum_i
\left(
\prod_{j\in N(i)}P_j
\right)
\sigma_i^x
\left(
\prod_{j\in N(i)}P_j
\right)
-\Delta\sum_i n_i,
]

其中

[
P_j=1-n_j.
]

一维最近邻版本就是 PXP 模型。它不仅定义哪些配置允许，还让不同 independent sets 之间发生量子跃迁。([arXiv][3])

因此简单的 overlap/counting network 只能给出：

* 受约束 Hilbert 空间维数；
* classical partition function；
* RK 型等权或 fugacity-weighted 状态；
* diagonal correlations。

它**不能单独给出**一般 Rydberg Hamiltonian 的：

* 基态；
* 激发谱；
* quantum scars；
* 实时间动力学；
* 相变点；
* 非对角相干。

这些需要真正优化 PEPS/MPS，或者收缩

[
\langle\Psi|H|\Psi\rangle
]

和时间演化网络。

---

# 8. 软 blockade 也可以处理

硬 blockade 是

[
B_{11}=0.
]

若相邻双激发只是能量很高而非完全禁止，可以改成有限温度权重

[
B_{n_i n_j}
===========

e^{-\beta V_{ij}n_i n_j}.
]

于是

[
Z
=

\sum_{\boldsymbol n}
\exp\left[
-\beta
\left(
\sum_{i<j}V_{ij}n_i n_j
-\mu\sum_i n_i
\right)
\right]
]

仍然是一个经典张量网络配分函数。

所以有连续对应：

[
\text{hard blockade}
\quad
V\rightarrow\infty
\quad\Longleftrightarrow\quad
B_{11}=0,
]

[
\text{soft blockade}
\quad
V<\infty
\quad\Longleftrightarrow\quad
B_{11}=e^{-\beta V}.
]

不过真实 van der Waals 相互作用 (V_{ij}\sim 1/r_{ij}^6) 是长程的。若保留所有长程作用，张量网络需要：

* 截断到有限作用距离；
* MPO/PEPO 近似；
* 辅助场分解；
* 或其他长程相互作用压缩。

---

# 9. 它可能比 N 皇后更适合做物理研究

N 皇后的最终目标主要是一个整数 (Q(N))。Rydberg blockade 的张量网络则可以自然输出一整套物理量：

## 配置计数

[
\mathcal N=#\operatorname{Ind}(G).
]

## 固定激发数的计数

[
\mathcal N_k
============

#{I:|I|=k}.
]

它是 independence polynomial 中 (z^k) 的系数。

## 激发密度

[
\rho
====

\frac1N z\partial_z\ln Z.
]

## 密度关联

[
\langle n_i n_j\rangle
======================

\frac1Z
\sum_I
n_i(I)n_j(I)z^{|I|}.
]

## 结构因子

[
S(\boldsymbol q)
================

\frac1N
\sum_{ij}
e^{i\boldsymbol q\cdot(\boldsymbol r_i-\boldsymbol r_j)}
\left(
\langle n_i n_j\rangle
-\langle n_i\rangle\langle n_j\rangle
\right).
]

## 最大填充和简并度

[
\alpha(G),\qquad g_{\rm MIS}.
]

## 缺陷生成函数

若允许 blockade violations 并赋予权重 (y)：

[
Z(z,y)
======

\sum_{\boldsymbol n}
z^{\sum_i n_i}
y^{\sum_{(i,j)}n_i n_j},
]

则可研究违反 blockade 的缺陷密度与涨落。

---

# 10. 对 N-Queens 优化经验的迁移

这也是很有意思的地方。

## 可以直接迁移的

### 局域约束编译

blockade 边张量只有 3 个非零元：

[
(0,0),\ (0,1),\ (1,0).
]

可以从显式张量自动编译合法 transition iterator，而不是扫描所有局域项。

### 对称 sector

可以按：

* 总激发数；
* 平移动量；
* 晶格空间群；
* 粒子数奇偶；
* 子晶格占据数；

分 sector 收缩。

### 组合计数与物理配分函数共用内核

相同 contraction engine 可以在不同 semiring 上工作：

* 普通整数：配置计数；
* 多项式：按激发数分辨计数；
* 浮点：经典配分函数；
* tropical：最大独立集；
* 复数：量子振幅。

### 自适应 merge/tail

前期合并相同边界状态，后期直接递归收缩剩余 constraint graph，可能同样有效。

---

## 不能照搬的

N 皇后每行必须恰好放一个皇后，因此逐行收缩自然编译成极强的 bitmask recurrence。

Rydberg blockade 通常允许一行放任意多个互不冲突的激发，因此：

* 每行合法模式数更大；
* 不同父边界可能产生更多 successor；
* 是否能像 N 皇后那样最终逼近 DFS，需要具体 benchmark；
* 对二维 hard-square，边界状态数仍可能指数增长。

不过由于 blockade 是最近邻局域约束，其 transfer operator 和 row patterns 通常比 N 皇后长程对角约束更规则，反而更适合：

* boundary MPS；
* CTMRG；
* transfer-matrix eigensolver；
* thermodynamic-limit iPEPS/iMPS。

---

# 结论

可以把 Rydberg blockade 写成局域约束 PEPS，并与全配置积态求内积：

[
\boxed{
\langle\Sigma|\Psi_{\rm bl}\rangle
==================================

#\operatorname{Ind}(G)
}
]

其物理意义包括：

[
\boxed{
\dim\mathcal H_{\rm blockade}
=============================

#\operatorname{Ind}(G)
}
]

以及加 fugacity 后：

[
\boxed{
\langle\Psi(z)|\Psi(z)\rangle
=============================

Z_{\rm hard\mbox{-}core}(z)
}
]

这对应受约束 Hilbert 空间维数、构型熵、hard-core lattice gas 配分函数、激发密度、关联函数和相变。

归一化的等权叠加

[
\sum_{I\in\operatorname{Ind}(G)}|I\rangle
]

还是一个真正的 RK 型受约束量子态。但对于含 Rabi 驱动的 PXP/Rydberg Hamiltonian，组合计数只描述其约束空间；要研究基态、scar 和动力学，还必须处理 independent sets 之间的非对角量子跃迁。

[1]: https://arxiv.org/abs/2202.09372?utm_source=chatgpt.com "Quantum Optimization of Maximum Independent Set using ..."
[2]: https://arxiv.org/pdf/2205.03718?utm_source=chatgpt.com "arXiv:2205.03718v3 [cond-mat.stat-mech] 23 Sep 2022"
[3]: https://arxiv.org/html/2311.08965v2?utm_source=chatgpt.com "Variational manifolds for ground states and scarred dynamics of ..."
[4]: https://arxiv.org/abs/2205.02508?utm_source=chatgpt.com "Tensor network investigation of the hard-square model"
[5]: https://arxiv.org/pdf/1010.2349?utm_source=chatgpt.com "arXiv:1010.2349v2 [physics.atom-ph] 23 Jan 2011"
